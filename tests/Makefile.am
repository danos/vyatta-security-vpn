TEST_EXTENSIONS = .t
TESTS_ENVIRONMENT = TZ=C; export TZ;
T_LOG_COMPILER = $(PERL)
AM_T_LOG_FLAGS = -Mstrict -w

TESTS = config.t
TESTS += mock_simple.t
TESTS += opmode.t
TESTS += prof_opmode.t
TESTS += util.t
TESTS += vti.t
TESTS += charon.t
